# ✅ RENDER DEPLOYMENT CONFIGURATION
# Following odin.fun pattern: Off-chain frontend deployment

services:
  - type: web
    name: AT_frontend
    env: node
    buildCommand: ./build.sh
    startCommand: npm run start
    envVars:
      - key: ATTICUS_CORE_CANISTER_ID
        value: q4oqk-hyaaa-aaaam-qd4la-cai
      - key: ICP_NETWORK
        value: ic
      - key: ICP_HOST
        value: https://ic0.app
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: true
    healthCheckPath: /
    autoDeploy: true
    plan: starter